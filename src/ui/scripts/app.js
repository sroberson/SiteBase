window.debounce = window.debounce || {};

define(function (require) {

    "use strict";

    var $ = require("jquery");



    (function(){


// **********************************************************************************************************
        // auto-executing code   (previously an init that gets returned)

        $.noop();

        // SetUp Listeners::



// **********************************************************************************************************

    }());
});
